% Figure: measured live-migration downtime for the acceptance runs.
% Columns of data/runs.dat: run index, downtime (ms), copy duration (s).
% The number of runs is not hard-coded: the axis follows data/runs.dat, which
% paper/update-results.py regenerates from the batch CSV.
\begin{tikzpicture}
\begin{axis}[
  width=\columnwidth, height=4.6cm,
  ybar, bar width=4.5pt,
  xlabel={acceptance run},
  ylabel={downtime (ms)},
  xmin=0.2, ymin=0, ymax=24,
  xtick distance=5,
  grid=major, grid style={gray!22},
  tick label style={font=\scriptsize},
  label style={font=\scriptsize},
  nodes near coords, nodes near coords style={font=\tiny},
  every node near coord/.append style={yshift=1pt},
]
  \addplot[fill=blue!35, draw=blue!60!black] table[x index=0, y index=1]
    {data/runs.dat};
\end{axis}
\end{tikzpicture}
